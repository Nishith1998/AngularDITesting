import { ItemsPerPagePipe } from './items-per-page.pipe';

describe('ItemsPerPagePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemsPerPagePipe();
    expect(pipe).toBeTruthy();
  });
});
